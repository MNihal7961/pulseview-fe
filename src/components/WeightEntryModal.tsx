import React from "react";

interface WeightEntryModalProps {
  action: "add" | "edit";
}
const WeightEntryModal: React.FC<WeightEntryModalProps> = () => {
  return <div>WeightEntryModal</div>;
};

export default WeightEntryModal;
